  <% if game_comment.favorited_by?(current_user) %>
     <%= link_to game_game_comment_favorites_path(@game,game_comment), data: {"turbolinks" => false}, method: :delete, remote: true do %>
        ♥<%= game_comment.favorites.count %> いいね
  <% end %>

    <% else %>
     <%= link_to game_game_comment_favorites_path(@game,game_comment), data: {"turbolinks" => false}, method: :post, remote: true do %>
        ♡<%= game_comment.favorites.count %> いいね
      <% end %>
  <% end %>
